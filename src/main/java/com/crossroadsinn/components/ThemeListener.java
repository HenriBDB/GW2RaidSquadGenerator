package com.crossroadsinn.components;

/**
 * A component that includes an updateTheme method which can
 * be called anytime the theme of the application changes.
 */
public interface ThemeListener {
    void updateTheme();
}
